{
  "name": "gym-management-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "dev:frontend": "npm run start --prefix gms-frontened",
    "dev:backend": "npm run start --prefix backend",
    "frontend": "npm run start --prefix gms-frontened",
    "backend": "npm run start --prefix backend",
    "build": "npm run install:all && npm run build --prefix gms-frontened",
    "install:all": "npm install --prefix backend && npm install --prefix gms-frontened",
    "render-build": "npm install --prefix backend && npm install --prefix gms-frontened && npm run build --prefix gms-frontened",
    "seed": "npm run seed --prefix backend"
  }
}
